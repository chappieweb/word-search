== Word Search

This is a very simple application that allows a user to enter a word without vowels, or spaces and will lookup words that could match, then displays a series of phrases that could match

Example:

cat owner

if you remove all the vowels you get

ctwnr

The app will find all phrases including cat owner that could be made with the specified string.